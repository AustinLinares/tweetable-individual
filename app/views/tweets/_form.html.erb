<p style="color: green"><%= notice %></p>

  <%= form_with(model: tweet, method: :post) do |f| %> 

  <div >
    <div data-turbo="false" class="tweet-form__container">
      <%= f.text_area :body, placeholder: "Remember to think before write...",
      autofocus: true, class:"tweet-form"%>
        <%= f.submit "Tweet", class:"tweet-button" %>
    </div>

  </div>

  <% end %>
